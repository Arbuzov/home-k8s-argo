apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability
  namespace: argo-cd
spec:
  project: default
  source:
    repoURL: https://github.com/Arbuzov/home-k8s-argo.git
    targetRevision: main
    path: observability
    directory:
      recurse: true
      # configmap*.yaml — для приложений, которым нужен монтируемый скрипт
      # (claude-status). Генерируется, не пишется руками — см. его README.
      include: '{project.yaml,*/application*.yaml,*/configmap*.yaml}'
      exclude: '{influxdb/application.yaml,prometheus/application.yaml,cloud-billing/application-stackdriver-exporter.yaml}'
  destination:
    server: https://kubernetes.default.svc
    namespace: argo-cd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
