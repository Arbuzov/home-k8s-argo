# Agent conventions

See [`CLAUDE.md`](CLAUDE.md) — the same conventions apply to every AI agent.

Key rule: **manifests carry no explanatory comments.** Rationale for a
non-obvious value goes in the service's (or group's) sibling `README.md`, never
in a `*.yaml` comment. Create the README if it doesn't exist.
